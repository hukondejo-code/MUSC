# Copilot Instructions

## Project Guidelines
- For Form3 loading simulation, simulate exactly the configured number of rows from Settings.ini. Skip simulation entirely when Settings.ini is missing. Use Thread.Sleep for pacing and display NEV_ values only in the loading text. The splash should simulate only the dynamic number of configured applications rather than the old fixed 8-tab system.